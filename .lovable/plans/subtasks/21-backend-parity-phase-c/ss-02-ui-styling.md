# SS-02: Demo Login UI Styling

Parent: 21-backend-parity-phase-c
Status: pending

## Context

This subtask covers the visual implementation of the `<DemoLoginPanel />` to ensure it feels like a native part of the system while remaining clearly identified as a preview-only tool.

## Acceptance Criteria

- [ ] Uses `surface-elevated` background token.
- [ ] Subtle animation on entry (slide up/fade).
- [ ] Mobile-responsive layout (stacking buttons if needed).
- [ ] Tooltips for identity descriptions.

## Affected Files

- `src/components/auth/DemoLoginPanel.tsx`
- `src/styles.css`
